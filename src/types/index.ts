// src/types/index.ts

// 1. Export alle ruwe types & Zod schema's (bevat nu de 'echte' RawHolding)
export * from './raw';

// 2. Export alle berekende types voor de UI en Engine
export * from './enriched';

// Optioneel: Export specifieke utilities als je die hebt